# Phase 4: Template System - Context

**Gathered:** 2026-03-29
**Status:** Ready for planning

<domain>
## Phase Boundary

Generate correct, runnable Dockerfiles, entrypoint scripts, and shell RC files from embedded Go templates. Implement deterministic container/network naming from repo paths. Templates render to `.zone/` cache directory. Docker build/run (Phase 5+) consumes these rendered files.

</domain>

<decisions>
## Implementation Decisions

### Git identity forwarding (DOC-14)
- Detection: shell out to `git config --global user.name` and `git config --global user.email` via os/exec
- If host has no git config: skip silently — set `.ForwardGitConfig = false`, omit git config lines from entrypoint
- Always-on behavior: no toggle in zone.toml. If host has git identity, forward it; if not, silently omit
- Partial config (only name or only email set): treat as missing — skip forwarding entirely

### Rendered template inspectability
- Template variable values logged at debug level only (visible with `--debug` or `ZONE_DEBUG=1`)
- Normal output shows nothing about template rendering
- Each rendered file gets a generation header comment: `# Generated by zone <version> -- do not edit`
- Template rendering errors follow spec's actionable error convention (Section 3.12): include the missing field and a hint pointing to zone.toml / global config

### Embed approach
- Switch from current `embed.FS` to individual string vars per spec Section 11:
  - `var DockerfileTmpl string`
  - `var EntrypointTmpl string`
  - `var ZoneBashrcTmpl string`
- Update `internal/cache/hash.go` (Phase 3 code) to use `templates.DockerfileTmpl` and `templates.EntrypointTmpl` string vars instead of `fs.ReadFile`
- Remove the `embed.FS` export — single access pattern throughout codebase

### Platform detection for macOS (DOC-15)
- Detection: `runtime.GOOS == "darwin"` + `user.Current().Username` (standard library)
- If detection fails: skip symlink silently (set `.MacOSUsername` to empty string, debug log the failure)
- Username sanitization: spec's `[a-zA-Z0-9_.-]` regex only — no additional length checks
- macOS only: no special handling for Docker Desktop on Linux
- Template data struct gets flat `MacOSUsername string` field — does not embed the full Platform struct (that's for Docker Manager in Phase 5+)

### Container/network naming (DOC-07)
- Implemented exactly per spec Section 12: `zone-<sanitized-repo-name>-<16-char-sha256-hash>`
- Network name: container name + `-net` suffix
- Container labels: `com.zone.managed`, `com.zone.repo-path`, `com.zone.harness`

### Security (DOC-05, DOC-06)
- Sudo scoped to exact commands per spec: `/usr/bin/apt-get, /usr/bin/apt, /usr/bin/pip*, /usr/bin/npm, /usr/local/bin/npm`
- Container security flags per spec: `no-new-privileges`, `CapDrop: ALL`, specific CapAdd list, pids limit

### Root/CI mode (DOC-16)
- When `HostUID == 0`: skip user creation entirely, no USER directive, no sudoers entry
- Template conditional handles this: `{{- if eq .HostUID 0 }}`

### Claude's Discretion
- Template data struct design (field names, organization, helper methods)
- Template rendering pipeline internals (parse once vs per-render, error wrapping)
- Test strategy for template rendering (golden file tests vs assertion-based)
- Exact generation header format and placement

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Template definitions
- `zone-spec.md` §11 (lines 947-1148) — Complete Dockerfile.tmpl, entrypoint.sh.tmpl, zone-bashrc.tmpl content with all template variables and security notes
- `zone-spec.md` §11 (lines 947-967) — Embed pattern: individual `//go:embed` string vars per template file

### Container naming and security
- `zone-spec.md` §12 (lines 1191-1237) — ContainerName/NetworkName/ContainerLabels Go code, Container Creation Security Flags

### Cache directory structure
- `zone-spec.md` §6 (lines 596-635) — `.zone/` directory layout showing where rendered templates are stored

### Error convention
- `zone-spec.md` §3.12 (lines 218-243) — Actionable error message format with hints

### Config types (consumed by templates)
- `zone-spec.md` §4 (lines 254-458) — Global and per-repo config keys that feed template variables
- `zone-spec.md` §5 (lines 543-595) — Harness-specific config keys

### Platform detection
- `zone-spec.md` §12 (lines 1275-1305) — Platform struct and detection logic

### Requirements
- `.planning/REQUIREMENTS.md` — DOC-01 through DOC-07, DOC-13 through DOC-16

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `pkg/templates/templates.go` — Has `//go:embed *.tmpl` and `var FS embed.FS`. Phase 4 migrates this to individual string vars per spec
- `pkg/templates/Dockerfile.tmpl` — Stub file exists, needs full spec content
- `pkg/templates/entrypoint.sh.tmpl` — Stub file exists, needs full spec content
- `pkg/templates/zone-bashrc.tmpl` — Stub file exists, needs full spec content
- `internal/config/types.go` — MergedConfig, ZoneConfig, GlobalConfig fully defined with typed fields (Phase 2)

### Established Patterns
- Stub-first development: `internal/docker/dockerfile.go`, `entrypoint.go`, `shellrc.go`, `naming.go`, `errors.go` all exist as stubs
- Error handling convention: `fmt.Errorf("context: %w", err)` throughout codebase
- Cache directory: `.zone/` structure established in Phase 3

### Integration Points
- `internal/cache/hash.go` — Currently uses `templates.FS` for cache hash computation; needs update to use string vars
- `internal/docker/` stubs — Template rendering functions will live here, consumed by Docker Manager in Phase 5
- `.zone/Dockerfile`, `.zone/entrypoint.sh`, `.zone/zone-bashrc` — Rendered output locations established by Phase 3's cache structure

</code_context>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches. User consistently chose recommended defaults, preferring simplicity (silent fallbacks, no extra toggles, flat data structures, minimal logging).

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 04-template-system*
*Context gathered: 2026-03-29*
