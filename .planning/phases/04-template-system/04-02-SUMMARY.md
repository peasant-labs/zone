---
phase: 04-template-system
plan: 02
subsystem: docker
tags: [go, templates, text/template, docker, entrypoint, shellrc, platform-detection]

# Dependency graph
requires:
  - phase: 04-template-system-01
    provides: "DockerfileTmpl, EntrypointTmpl, ZoneBashrcTmpl string vars in pkg/templates"
provides:
  - "DockerfileData struct and RenderDockerfile() in internal/docker/dockerfile.go"
  - "EntrypointData struct and RenderEntrypoint() in internal/docker/entrypoint.go"
  - "ShellRCData struct and RenderShellRC() in internal/docker/shellrc.go"
  - "HostUID(), MacOSUsername(), DetectGitIdentity() in internal/docker/platform.go"
  - "templateFuncs() FuncMap with join for all templates"
  - "injectGenerationComment() header injection (line 2 after syntax directive for Dockerfile, line 1 for others)"
affects: [05-docker-integration, 06-launch-command, phase-5-harness]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "text/template with FuncMap: templateFuncs() defined once in dockerfile.go, shared across package"
    - "Generation header injection: syntax directive on line 1 → header on line 2 for Dockerfile; prepend for shell files"
    - "Platform detection: runtime.GOOS guard for macOS-only paths; user.Current() for UID"
    - "Git identity forwarding: both-or-nothing rule — forward=false if either name or email is empty"

key-files:
  created:
    - internal/docker/platform.go
    - tests/platform_test.go
    - tests/template_render_test.go
  modified:
    - internal/docker/dockerfile.go
    - internal/docker/entrypoint.go
    - internal/docker/shellrc.go

key-decisions:
  - "templateFuncs() and injectGenerationComment() defined once in dockerfile.go and reused by entrypoint.go and shellrc.go — no duplication"
  - "zone-bashrc.tmpl template has a static # Generated header; RenderShellRC prepends a versioned header on top — two headers acceptable per plan guidance"
  - "MacOSUsername() returns empty string on any error (including non-darwin) — silent failure per spec"
  - "DetectGitIdentity() both-or-nothing: partial config (name without email or vice versa) treated as not configured"

patterns-established:
  - "Render function signature: Render*(data *Data, version string) (string, error)"
  - "Template FuncMap: join = strings.Join, defined centrally in dockerfile.go"
  - "Generation header format: '# Generated by zone {version} -- do not edit'"

requirements-completed: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-13, DOC-14, DOC-15, DOC-16]

# Metrics
duration: 3min
completed: 2026-03-29
---

# Phase 4 Plan 02: Template Render Pipeline Summary

**Three render functions (RenderDockerfile, RenderEntrypoint, RenderShellRC) with data structs, FuncMap, generation header injection, platform detection (HostUID, MacOSUsername, DetectGitIdentity), and 22 passing tests covering all DOC requirements**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-29T18:42:10Z
- **Completed:** 2026-03-29T18:45:39Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- Three template render functions with full data structs matching all template variables
- Platform detection: POSIX UID via os/user, macOS username with regex sanitization, git identity with both-or-nothing rule
- Generation header injection: line 2 for Dockerfile (after `# syntax=` directive), line 1 prepended for shell files
- 19 render tests + 3 platform tests, all passing; full prior test suite (hash, cache, naming, validate) still green

## Task Commits

Each task was committed atomically:

1. **Task 1: Platform detection and git identity functions** - `669f775` (feat)
2. **Task 2: DockerfileData, EntrypointData, ShellRCData structs and render functions** - `7823187` (feat)
3. **Task 3: Template rendering tests** - `1124937` (feat)

**Plan metadata:** _(docs commit below)_

## Files Created/Modified
- `internal/docker/platform.go` - HostUID(), MacOSUsername(), DetectGitIdentity(), runGitConfig()
- `internal/docker/dockerfile.go` - DockerfileData struct, RenderDockerfile(), templateFuncs(), injectGenerationComment()
- `internal/docker/entrypoint.go` - EntrypointData struct, RenderEntrypoint()
- `internal/docker/shellrc.go` - ShellRCData struct, RenderShellRC()
- `tests/platform_test.go` - 3 platform detection tests
- `tests/template_render_test.go` - 19 render tests covering all DOC requirements

## Decisions Made
- `templateFuncs()` and `injectGenerationComment()` defined once in `dockerfile.go` — all three render files share these package-level helpers without duplication
- `zone-bashrc.tmpl` has its own static `# Generated by zone -- do not edit` header on line 1; `RenderShellRC` prepends a versioned header on top — plan guidance explicitly allowed this double-header
- `MacOSUsername()` returns `""` silently on failure (no error logging) per spec
- `DetectGitIdentity()` both-or-nothing: partial git config (name without email) returns `forward=false`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

All files created, all commits exist.

## Next Phase Readiness
- All render functions ready for Phase 5 (harness system) to call with harness-specific data
- `RenderDockerfile`, `RenderEntrypoint`, `RenderShellRC` are the primary interface for file generation going forward
- `HostUID()` and `MacOSUsername()` used by Phase 5 to populate `DockerfileData` at runtime

---
*Phase: 04-template-system*
*Completed: 2026-03-29*
