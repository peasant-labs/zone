---
phase: 01-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - cmd/root.go
  - cmd/init.go
  - cmd/launch.go
  - cmd/join.go
  - cmd/exec.go
  - cmd/shell.go
  - cmd/build.go
  - cmd/stop.go
  - cmd/restart.go
  - cmd/ls.go
  - cmd/logs.go
  - cmd/clean.go
  - cmd/destroy.go
  - cmd/status.go
  - cmd/config.go
  - cmd/validate.go
  - internal/config/types.go
  - internal/config/harness_config.go
  - internal/config/config.go
  - internal/config/global.go
  - internal/config/merge.go
  - internal/config/validate.go
  - internal/cache/cache.go
  - internal/cache/hash.go
  - internal/cache/lock.go
  - internal/docker/manager.go
  - internal/docker/dockerfile.go
  - internal/docker/entrypoint.go
  - internal/docker/shellrc.go
  - internal/docker/naming.go
  - internal/docker/network.go
  - internal/docker/platform.go
  - internal/docker/errors.go
  - internal/network/firewall.go
  - internal/network/rules.go
  - internal/network/matcher.go
  - internal/harness/harness.go
  - internal/harness/claude_code.go
  - internal/harness/opencode.go
  - internal/harness/gemini_cli.go
  - internal/harness/aider.go
  - internal/harness/codex_cli.go
  - internal/harness/custom.go
  - internal/tui/init_wizard.go
  - internal/tui/build_progress.go
  - internal/tui/status_view.go
  - internal/tui/log_viewer.go
  - pkg/templates/templates.go
  - pkg/templates/Dockerfile.tmpl
  - pkg/templates/entrypoint.sh.tmpl
  - pkg/templates/zone-bashrc.tmpl
  - tests/config_merge_test.go
  - tests/harness_validate_test.go
  - tests/validate_test.go
  - tests/naming_test.go
  - tests/matcher_test.go
  - tests/hash_test.go
autonomous: true
requirements:
  - DX-10

must_haves:
  truths:
    - "go build ./... succeeds from a clean checkout with no manual setup"
    - "go test ./... runs and exits 0"
    - "zone --help shows all 15 subcommands with correct Short descriptions"
    - "zone launch returns 'Error: not implemented' with exit code 1"
    - "Every internal package has a valid Go stub file that compiles"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
      contains: "module github.com/peasant-labs/zone"
    - path: "main.go"
      provides: "Entry point with version vars"
      contains: "func main()"
    - path: "cmd/root.go"
      provides: "Cobra root command with 15 subcommands and 4 global flags"
      exports: ["Execute", "rootCmd"]
    - path: "cmd/launch.go"
      provides: "launch subcommand stub"
      contains: "not implemented"
    - path: "pkg/templates/templates.go"
      provides: "Embedded template FS"
      contains: "go:embed"
    - path: "internal/config/types.go"
      provides: "Config package stub"
      contains: "package config"
    - path: "internal/docker/errors.go"
      provides: "Docker package stub"
      contains: "package docker"
  key_links:
    - from: "main.go"
      to: "cmd/root.go"
      via: "cmd.Execute() call"
      pattern: "cmd\\.Execute\\(\\)"
    - from: "cmd/root.go"
      to: "cmd/*.go"
      via: "rootCmd.AddCommand()"
      pattern: "rootCmd\\.AddCommand"
    - from: "pkg/templates/templates.go"
      to: "pkg/templates/*.tmpl"
      via: "go:embed directive"
      pattern: "go:embed"
---

<objective>
Initialize the Go module with Cobra CLI skeleton and all package stubs from spec Section 7.

Purpose: Establish the complete project structure so `go build ./...` and `go test ./...` both succeed. This is the foundation every subsequent phase imports from.
Output: Compilable Go project with 15 wired Cobra subcommands and ~40 stub files across 8 packages.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold/01-CONTEXT.md
@.planning/phases/01-project-scaffold/01-RESEARCH.md
@.planning/phases/01-project-scaffold/01-UI-SPEC.md
@zone-spec.md (Section 7 — project structure, Section 8 — error handling convention)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and create Cobra CLI skeleton</name>
  <files>go.mod, go.sum, main.go, cmd/root.go, cmd/init.go, cmd/launch.go, cmd/join.go, cmd/exec.go, cmd/shell.go, cmd/build.go, cmd/stop.go, cmd/restart.go, cmd/ls.go, cmd/logs.go, cmd/clean.go, cmd/destroy.go, cmd/status.go, cmd/config.go, cmd/validate.go</files>
  <read_first>
    - zone-spec.md (Section 7 lines 637-715 for project structure, Section 8 lines 731-775 for error convention)
    - .planning/phases/01-project-scaffold/01-RESEARCH.md (Pattern 1: Cobra Root Command, Pattern 2: Stub files)
    - .planning/phases/01-project-scaffold/01-UI-SPEC.md (Copywriting Contract — exact Short descriptions and global flag descriptions)
  </read_first>
  <action>
1. Run `go mod init github.com/peasant-labs/zone` in the project root.

2. Run `go get github.com/spf13/cobra@v1.10.2` to add the Cobra dependency.

3. Create `main.go` with these exact contents:
   - Package `main`
   - Import `os` and `github.com/peasant-labs/zone/cmd`
   - Three package-level vars for GoReleaser ldflags injection:
     ```
     var version = "dev"
     var commit  = "none"
     var date    = "unknown"
     ```
   - `func main()` that calls `cmd.Execute()` and exits with code 1 on error

4. Create `cmd/root.go`:
   - Package `cmd`
   - Import `github.com/spf13/cobra`
   - `var rootCmd = &cobra.Command{...}` with:
     - `Use: "zone"`
     - `Short: "Sandboxed Docker workspaces for LLM coding agents"`
     - `Long: "Zone generates and manages isolated Docker workspaces for LLM coding agents. Run zone launch in any repo to get a sandboxed container with zero manual Docker configuration."`
   - `func Execute() error { return rootCmd.Execute() }`
   - `func init()` that:
     - Calls `rootCmd.AddCommand()` with all 15 subcommand vars: `initCmd, launchCmd, joinCmd, execCmd, shellCmd, buildCmd, stopCmd, restartCmd, lsCmd, logsCmd, cleanCmd, destroyCmd, statusCmd, configCmd, validateCmd`
     - Registers 4 global persistent flags:
       - `--verbose` / `-v` (bool, false): "Increase output verbosity"
       - `--debug` (bool, false): "Maximum verbosity including raw API responses"
       - `--quiet` / `-q` (bool, false): "Suppress all non-essential output"
       - `--plain` (bool, false): "Disable TUI and use plain text output"

5. Create each of the 15 `cmd/*.go` files following this exact pattern (one file per command). Each file:
   - Package `cmd`
   - Imports `fmt` and `github.com/spf13/cobra`
   - Declares a package-level `var {name}Cmd = &cobra.Command{...}` with:
     - `Use`, `Aliases` (if any), `Short` fields from the table below
     - `RunE` that returns `fmt.Errorf("not implemented")` — exactly this string, no variation

   Command table (exact values from UI-SPEC.md Copywriting Contract):

   | File | Var | Use | Aliases | Short |
   |------|-----|-----|---------|-------|
   | cmd/init.go | initCmd | "init" | (none) | "Scaffold a zone.toml in the current directory" |
   | cmd/launch.go | launchCmd | "launch" | ["up"] | "Build (if needed) and attach to the container" |
   | cmd/join.go | joinCmd | "join" | (none) | "Attach a new shell to a running container" |
   | cmd/exec.go | execCmd | "exec" | (none) | "Run a one-off command inside the running container" |
   | cmd/shell.go | shellCmd | "shell" | (none) | "Open an interactive shell in the container" |
   | cmd/build.go | buildCmd | "build" | (none) | "Force-rebuild the Docker image without launching" |
   | cmd/stop.go | stopCmd | "stop" | ["down"] | "Stop and remove the container and network" |
   | cmd/restart.go | restartCmd | "restart" | (none) | "Stop and relaunch the container" |
   | cmd/ls.go | lsCmd | "ls" | ["list"] | "List all zone containers across all repos" |
   | cmd/logs.go | logsCmd | "logs" | ["log"] | "View harness output" |
   | cmd/clean.go | cleanCmd | "clean" | (none) | "Remove the .zone/ cache directory" |
   | cmd/destroy.go | destroyCmd | "destroy" | (none) | "Fully tear down container, image, network, and cache" |
   | cmd/status.go | statusCmd | "status" | ["st"] | "Show container state, harness, uptime, and resources" |
   | cmd/config.go | configCmd | "config" | (none) | "Show effective merged configuration with source annotations" |
   | cmd/validate.go | validateCmd | "validate" | (none) | "Check zone.toml validity without starting anything" |

6. Run `go mod tidy` to clean up go.sum.

7. Verify: `go build ./...` must succeed with exit code 0.
  </action>
  <verify>
    <automated>cd /workspace/zone && go build ./... && go run . --help | grep -c "Available Commands" && go run . launch 2>&1 | grep "not implemented"</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module github.com/peasant-labs/zone`
    - go.mod contains `go 1.24` (or higher toolchain directive)
    - go.mod contains `github.com/spf13/cobra v1.10.2` (or compatible)
    - main.go contains `var version = "dev"`
    - main.go contains `cmd.Execute()`
    - cmd/root.go contains `rootCmd.AddCommand(`
    - cmd/root.go contains `Sandboxed Docker workspaces for LLM coding agents`
    - cmd/root.go contains `PersistentFlags().BoolP("verbose", "v"`
    - cmd/root.go contains `PersistentFlags().Bool("debug"`
    - cmd/root.go contains `PersistentFlags().BoolP("quiet", "q"`
    - cmd/root.go contains `PersistentFlags().Bool("plain"`
    - cmd/launch.go contains `Aliases: []string{"up"}`
    - cmd/launch.go contains `fmt.Errorf("not implemented")`
    - cmd/stop.go contains `Aliases: []string{"down"}`
    - cmd/ls.go contains `Aliases: []string{"list"}`
    - cmd/logs.go contains `Aliases: []string{"log"}`
    - cmd/status.go contains `Aliases: []string{"st"}`
    - All 15 cmd/*.go files exist (init, launch, join, exec, shell, build, stop, restart, ls, logs, clean, destroy, status, config, validate)
    - `go build ./...` exits 0
    - `go run . --help` output contains all 15 subcommand names
    - `go run . launch` outputs "Error: not implemented"
  </acceptance_criteria>
  <done>Go module initialised, main.go wired to cmd.Execute(), all 15 Cobra subcommands registered with exact Short descriptions and aliases from UI-SPEC, `go build ./...` succeeds, `zone --help` lists all commands, every subcommand returns "not implemented".</done>
</task>

<task type="auto">
  <name>Task 2: Create all internal, pkg, and test stub files</name>
  <files>internal/config/types.go, internal/config/harness_config.go, internal/config/config.go, internal/config/global.go, internal/config/merge.go, internal/config/validate.go, internal/cache/cache.go, internal/cache/hash.go, internal/cache/lock.go, internal/docker/manager.go, internal/docker/dockerfile.go, internal/docker/entrypoint.go, internal/docker/shellrc.go, internal/docker/naming.go, internal/docker/network.go, internal/docker/platform.go, internal/docker/errors.go, internal/network/firewall.go, internal/network/rules.go, internal/network/matcher.go, internal/harness/harness.go, internal/harness/claude_code.go, internal/harness/opencode.go, internal/harness/gemini_cli.go, internal/harness/aider.go, internal/harness/codex_cli.go, internal/harness/custom.go, internal/tui/init_wizard.go, internal/tui/build_progress.go, internal/tui/status_view.go, internal/tui/log_viewer.go, pkg/templates/templates.go, pkg/templates/Dockerfile.tmpl, pkg/templates/entrypoint.sh.tmpl, pkg/templates/zone-bashrc.tmpl, tests/config_merge_test.go, tests/harness_validate_test.go, tests/validate_test.go, tests/naming_test.go, tests/matcher_test.go, tests/hash_test.go</files>
  <read_first>
    - zone-spec.md (Section 7 lines 637-715 for complete file listing and package/import rules)
    - .planning/phases/01-project-scaffold/01-RESEARCH.md (Pattern 2: Stub Internal Package Files, Pattern for go:embed, Anti-Patterns about empty templates and stub imports)
    - go.mod (to confirm module path for test imports)
  </read_first>
  <action>
1. Create all `internal/` stub files. Each file has ONLY a package declaration and a doc comment. NO imports, NO type definitions, NO function signatures. The pattern for every file:

   ```go
   // Package {pkgname} provides {description}.
   package {pkgname}
   ```

   Exact file-to-content mapping:

   **internal/config/** (package `config`):
   - `types.go`: "Package config provides TOML configuration parsing, merging, and validation for zone."
   - `harness_config.go`: doc comment "harness_config.go defines the typed HarnessConfig struct for per-harness configuration."  (still `package config`, NOT a separate package)
   - `config.go`: doc comment "config.go handles TOML parsing with strict decoding."
   - `global.go`: doc comment "global.go reads and writes global config from ~/.config/zone/config.toml."
   - `merge.go`: doc comment "merge.go implements the two-tier config merge strategy."
   - `validate.go`: doc comment "validate.go validates config values including dangerous mount detection."

   **internal/cache/** (package `cache`):
   - `cache.go`: "Package cache manages the .zone/ directory, config hashing, and build log storage."
   - `hash.go`: doc comment "hash.go computes the full cache hash from config, templates, and zone version."
   - `lock.go`: doc comment "lock.go provides flock-based file locking for concurrent access protection."

   **internal/docker/** (package `docker`):
   - `manager.go`: "Package docker provides Docker container lifecycle management via the Docker SDK."
   - `dockerfile.go`: doc comment "dockerfile.go generates Dockerfiles from Go templates."
   - `entrypoint.go`: doc comment "entrypoint.go generates container entrypoint scripts from templates."
   - `shellrc.go`: doc comment "shellrc.go generates shell RC files with aliases and welcome messages."
   - `naming.go`: doc comment "naming.go implements deterministic container and network naming from repo paths."
   - `network.go`: doc comment "network.go manages Docker network creation and destruction."
   - `platform.go`: doc comment "platform.go detects host platform capabilities (macOS, Linux, rootless Docker)."
   - `errors.go`: doc comment "errors.go defines sentinel errors for the docker package."

   **internal/network/** (package `network`):
   - `firewall.go`: "Package network provides host-side iptables rule generation and management."
   - `rules.go`: doc comment "rules.go parses network allow/deny rules from configuration."
   - `matcher.go`: doc comment "matcher.go implements hostname glob matching for network rules."

   **internal/harness/** (package `harness`):
   - `harness.go`: "Package harness defines the harness interface, registry, and base implementation."
   - `claude_code.go`: doc comment "claude_code.go implements the claude-code harness."
   - `opencode.go`: doc comment "opencode.go provides a stub for the opencode harness."
   - `gemini_cli.go`: doc comment "gemini_cli.go provides a stub for the gemini-cli harness."
   - `aider.go`: doc comment "aider.go provides a stub for the aider harness."
   - `codex_cli.go`: doc comment "codex_cli.go provides a stub for the codex-cli harness."
   - `custom.go`: doc comment "custom.go implements user-defined harnesses from config."

   **internal/tui/** (package `tui`):
   - `init_wizard.go`: "Package tui provides BubbleTea terminal UI components for zone."
   - `build_progress.go`: doc comment "build_progress.go implements the Docker build progress display."
   - `status_view.go`: doc comment "status_view.go implements the live container status display."
   - `log_viewer.go`: doc comment "log_viewer.go implements the log viewer with follow mode."

2. Create `pkg/templates/templates.go` with go:embed directive:
   ```go
   // Package templates provides embedded Dockerfile and script templates for zone containers.
   package templates

   import "embed"

   //go:embed *.tmpl
   var FS embed.FS
   ```

3. Create three placeholder `.tmpl` files in `pkg/templates/`. Each must contain at least a comment line so they are non-empty (go:embed matches non-empty files fine; the key requirement is the files EXIST):
   - `Dockerfile.tmpl`: content `# Zone Dockerfile template — populated in Phase 4`
   - `entrypoint.sh.tmpl`: content `# Zone entrypoint template — populated in Phase 4`
   - `zone-bashrc.tmpl`: content `# Zone shell RC template — populated in Phase 4`

4. Create test stub files in `tests/` directory. Each file must compile but declare no test functions. Use this exact pattern for each:

   **IMPORTANT:** Go test files in a `tests/` directory at the project root must declare their own package. Use `package tests` for all of them. They must NOT import any internal packages (those don't exist as real code yet). Each file:

   ```go
   // {description}
   package tests
   ```

   Files:
   - `tests/config_merge_test.go`: "Tests for config merge logic."
   - `tests/harness_validate_test.go`: "Tests for harness-specific config validation."
   - `tests/validate_test.go`: "Tests for dangerous mount detection and symlink resolution."
   - `tests/naming_test.go`: "Tests for deterministic container name generation."
   - `tests/matcher_test.go`: "Tests for hostname glob matching."
   - `tests/hash_test.go`: "Tests for cache hash computation."

5. Run `go mod tidy` to ensure go.sum is clean (the only dependency should be cobra and its transitive deps; no heavy deps like docker SDK or bubbletea since stubs don't import them).

6. Verify: `go build ./...` and `go test ./...` must both exit 0.
  </action>
  <verify>
    <automated>cd /workspace/zone && go build ./... && go test ./... && test -f pkg/templates/Dockerfile.tmpl && test -f pkg/templates/entrypoint.sh.tmpl && test -f pkg/templates/zone-bashrc.tmpl</automated>
  </verify>
  <acceptance_criteria>
    - internal/config/types.go contains `package config`
    - internal/config/harness_config.go contains `package config`
    - internal/config/config.go contains `package config`
    - internal/config/global.go contains `package config`
    - internal/config/merge.go contains `package config`
    - internal/config/validate.go contains `package config`
    - internal/cache/cache.go contains `package cache`
    - internal/cache/hash.go contains `package cache`
    - internal/cache/lock.go contains `package cache`
    - internal/docker/manager.go contains `package docker`
    - internal/docker/dockerfile.go contains `package docker`
    - internal/docker/entrypoint.go contains `package docker`
    - internal/docker/shellrc.go contains `package docker`
    - internal/docker/naming.go contains `package docker`
    - internal/docker/network.go contains `package docker`
    - internal/docker/platform.go contains `package docker`
    - internal/docker/errors.go contains `package docker`
    - internal/network/firewall.go contains `package network`
    - internal/network/rules.go contains `package network`
    - internal/network/matcher.go contains `package network`
    - internal/harness/harness.go contains `package harness`
    - internal/harness/claude_code.go contains `package harness`
    - internal/harness/custom.go contains `package harness`
    - internal/tui/init_wizard.go contains `package tui`
    - internal/tui/build_progress.go contains `package tui`
    - internal/tui/status_view.go contains `package tui`
    - internal/tui/log_viewer.go contains `package tui`
    - pkg/templates/templates.go contains `//go:embed`
    - pkg/templates/templates.go contains `var FS embed.FS`
    - pkg/templates/Dockerfile.tmpl exists and is non-empty
    - pkg/templates/entrypoint.sh.tmpl exists and is non-empty
    - pkg/templates/zone-bashrc.tmpl exists and is non-empty
    - tests/config_merge_test.go contains `package tests`
    - tests/naming_test.go contains `package tests`
    - tests/matcher_test.go contains `package tests`
    - `go build ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>All 31 internal stub files exist with correct package declarations, pkg/templates has go:embed FS with 3 placeholder .tmpl files, 6 test stubs compile cleanly, `go build ./...` and `go test ./...` both exit 0.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `go build ./...` exits 0 — all packages compile
2. `go test ./...` exits 0 — test stubs have no failures
3. `go run . --help` shows all 15 subcommands with correct Short descriptions
4. `go run . launch` outputs "Error: not implemented" with exit code 1
5. Every file listed in spec Section 7 exists in the project tree
</verification>

<success_criteria>
- `go build ./...` succeeds from a clean checkout
- `go test ./...` runs and exits 0
- `zone --help` lists 15 subcommands with exact Short descriptions from UI-SPEC
- All 4 global flags (--verbose, --debug, --quiet, --plain) appear in help
- All command aliases work (up, down, list, log, st)
- Every package directory from spec Section 7 exists with at least one .go file
- pkg/templates/templates.go compiles with go:embed pointing to 3 .tmpl files
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold/01-01-SUMMARY.md`
</output>
