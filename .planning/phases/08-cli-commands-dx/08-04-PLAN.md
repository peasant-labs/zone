---
phase: 08-cli-commands-dx
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - cmd/init.go
  - cmd/errors.go
  - cmd/launch.go
  - cmd/join.go
  - cmd/shell.go
  - tests/cli_commands_test.go
  - tests/cli_dx_test.go
autonomous: false
requirements: [CLI-01, DX-02, DX-04, DX-06, DX-09]
gap_closure: true

must_haves:
  truths:
    - "`zone init` without `--harness` remains explicitly deferred to Phase 9 interactive flow (per D-02), with a clear remediation message"
    - "Fallback/unclassified CLI failures include actionable remediation guidance on stderr"
    - "All command help output satisfies the 2-4 usage-example contract, including launch/join/shell"
    - "Ctrl+C during `zone launch` still propagates signal behavior and container lifecycle expected by DX-04/DX-06"
  artifacts:
    - path: "cmd/init.go"
      provides: "Phase-8 init guardrails and deferred interactive guidance"
      contains: "interactive mode requires a terminal"
    - path: "cmd/errors.go"
      provides: "Fallback remediation text for unknown and unknown-keys errors"
      contains: "Run `zone --help`"
    - path: "tests/cli_dx_test.go"
      provides: "Automated guard that enforces 2-4 examples per command"
      contains: "TestHelpExamples"
  key_links:
    - from: "cmd/errors.go"
      to: "main.go"
      via: "MapError(err) remediation + exit code routing"
      pattern: "MapError"
    - from: "tests/cli_dx_test.go"
      to: "cmd/launch.go, cmd/join.go, cmd/shell.go"
      via: "`--help` assertions for example-count contract"
      pattern: "Examples:"
---

<objective>
Close remaining Phase 8 verification gaps by fixing DX-02 fallback remediation coverage, normalizing DX-09 example-count contract, and codifying the Phase-8 `zone init` non-interactive boundary per locked user decision D-02.

Purpose: Finish code-level gap closure without violating locked decisions, then run the required human runtime checks for signal/lifecycle truths that cannot be fully proven by static tests.
Output: Updated CLI error/help/init behavior, strengthened integration tests, and a final checkpoint script for DX-04/DX-06 runtime verification.
</objective>

<execution_context>
@/home/claude/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/claude/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-commands-dx/08-CONTEXT.md
@.planning/phases/08-cli-commands-dx/08-VERIFICATION.md
@.planning/phases/08-cli-commands-dx/08-03-SUMMARY.md
@zone-spec.md

<interfaces>
From cmd/errors.go:
```go
func mapError(err error) (string, int)
var MapError = mapError
```

From cmd/init.go:
```go
RunE: func(cmd *cobra.Command, args []string) error {
    harnessName, _ := cmd.Flags().GetString("harness")
    if harnessName == "" {
        return fmt.Errorf("interactive mode requires a terminal. Use `--harness <name>` for non-interactive init")
    }
}
```

From tests/cli_dx_test.go:
```go
func TestHelpExamples(t *testing.T)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codify Phase-8 init boundary for CLI-01 per locked D-02 and add regression coverage</name>
  <files>cmd/init.go, tests/cli_commands_test.go</files>
  <read_first>
    - cmd/init.go
    - tests/cli_commands_test.go
    - .planning/phases/08-cli-commands-dx/08-CONTEXT.md
    - .planning/phases/08-cli-commands-dx/08-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: `zone init` without `--harness` exits non-zero and includes deferred interactive guidance for Phase 8 (per D-02).
    - Test 2: `zone init --harness claude-code` still scaffolds zone.toml and remains unaffected.
    - Test 3: Existing `zone.toml` path still returns the overwrite-prevention message.
  </behavior>
  <action>
Keep Phase 8 behavior non-interactive exactly per locked user decision D-02 (do NOT add prompt-based or TUI-based harness selection here). In `cmd/init.go`, keep the no-`--harness` guard, but make the message explicitly mention that interactive selection is deferred to Phase 9 so verification cannot misinterpret this path as accidental omission.

In `tests/cli_commands_test.go`, strengthen `TestInitNoHarness` into a contract test that asserts:
1) non-zero exit,
2) current guidance string,
3) explicit deferred wording for Phase 9/interactive wizard,
4) stderr routing remains correct for this class of UX guidance.

Also keep `TestInitCreatesZoneToml` and `TestInitExistingZoneToml` green to prove no regression.
  </action>
  <verify>
    <automated>go test ./tests -run 'TestInitNoHarness|TestInitCreatesZoneToml|TestInitExistingZoneToml' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/init.go` retains a `harnessName == ""` guard (no interactive implementation added in this phase).
    - `cmd/init.go` contains deferred wording that points interactive init to Phase 9 (per D-02).
    - `tests/cli_commands_test.go` asserts no-harness path is non-zero and checks deferred interactive guidance text.
    - `go test ./tests -run 'TestInitNoHarness|TestInitCreatesZoneToml|TestInitExistingZoneToml' -count=1` exits 0.
  </acceptance_criteria>
  <done>CLI-01 scope mismatch is reconciled for Phase 8 by codifying the deferred interactive boundary and locking it with regression tests (per D-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close DX-02 and DX-09 gaps (fallback remediation + 2-4 help examples enforcement)</name>
  <files>cmd/errors.go, cmd/launch.go, cmd/join.go, cmd/shell.go, tests/cli_dx_test.go</files>
  <read_first>
    - cmd/errors.go
    - cmd/launch.go
    - cmd/join.go
    - cmd/shell.go
    - tests/cli_dx_test.go
    - .planning/phases/08-cli-commands-dx/08-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Unclassified/fallback errors now include a concrete remediation hint (not bare `Error: <err>`).
    - Test 2: Unknown-keys config errors still map to exit code 2 and include actionable remediation.
    - Test 3: `launch`, `join`, and `shell` help now each render between 2 and 4 examples.
    - Test 4: Example-count guard fails if any command drifts below 2 or above 4 in future edits.
  </behavior>
  <action>
In `cmd/errors.go`, keep existing sentinel mappings, then fix the two partial branches from verification:
1) Unknown-keys (`errors.As(err, *config.UnknownKeysError)`) must append explicit remediation guidance (e.g., run `zone validate`, fix suggested keys, re-run command).
2) Default fallback branch must include a generic-but-actionable hint (e.g., rerun with `--debug`, check `zone --help`, and include next-step context), while retaining exit code 1.

In `cmd/launch.go`, trim examples to at most 4 lines.
In `cmd/join.go` and `cmd/shell.go`, expand examples to at least 2 lines each.

In `tests/cli_dx_test.go`, upgrade `TestHelpExamples` so it validates count bounds (2-4) for each command instead of only checking the presence of an `Examples:` header. Add/extend tests that assert fallback remediation text appears for generic and unknown-keys error paths.
  </action>
  <verify>
    <automated>go test ./tests -run 'TestHelpExamples|TestRemediationHintOnStderr|TestExitCode2OnBadConfig' -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/errors.go` unknown-keys branch includes an actionable remediation instruction and still returns exit code 2.
    - `cmd/errors.go` default fallback branch includes actionable remediation instruction and returns exit code 1.
    - `cmd/launch.go` Example block has 2-4 usage lines.
    - `cmd/join.go` Example block has 2-4 usage lines.
    - `cmd/shell.go` Example block has 2-4 usage lines.
    - `tests/cli_dx_test.go` enforces numeric example bounds for all 15 commands.
    - `go test ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>All remaining automated DX gaps from verification are closed, and regressions are guarded by tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human runtime verification for DX-04 and DX-06 behavior</name>
  <what-built>Signal/context plumbing is already present in code; this checkpoint validates real terminal + container behavior that automated tests cannot reliably prove in CI.</what-built>
  <action>Execute the runtime verification sequence after Tasks 1-2 automated checks pass; capture observed behavior and block completion unless both runtime truths are confirmed or concrete failure evidence is recorded for replanning.</action>
  <how-to-verify>
    1. In a repo with valid `zone.toml`, run `zone launch` and wait until harness prompt appears.
    2. Press Ctrl+C once.
    3. Confirm CLI exits and run `zone ls --running` to verify container is still alive (DX-04).
    4. Run `zone launch` again; exit harness naturally (for example using harness-specific exit command).
    5. Confirm `zone launch` exits 0 and container lifecycle matches expected DX-06 behavior.
    6. Capture observed output and any discrepancies in the plan summary.
  </how-to-verify>
  <verify>
    <automated>go test ./tests -run 'TestAliases|TestGlobalFlags|TestLogsBuildFlag|TestValidateExitZero|TestConfigShowsMerged' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms Ctrl+C behavior matches DX-04 expectation (SIGINT path, container remains available).
    - Human confirms harness natural-exit behavior matches DX-06 expectation.
    - If either check fails, executor records exact failure mode and stops for follow-up planning.
  </acceptance_criteria>
  <resume-signal>Type "approved" if both runtime checks pass, or provide failure details for replanning.</resume-signal>
  <done>Manual runtime truths are validated or captured as concrete follow-up gaps.</done>
</task>

</tasks>

<verification>
Run automated checks first:

- `go test ./tests -run 'TestInitNoHarness|TestInitCreatesZoneToml|TestInitExistingZoneToml|TestHelpExamples|TestRemediationHintOnStderr|TestExitCode2OnBadConfig' -count=1`
- `go test ./... -count=1`

Then run the human checkpoint for DX-04 and DX-06.
</verification>

<success_criteria>
- No bare fallback error output remains in cmd/errors.go.
- launch/join/shell help examples satisfy the 2-4 contract and are enforced by tests.
- Phase-8 init behavior stays aligned with locked D-02 (interactive deferred) and is covered by tests.
- Human runtime verification for DX-04 and DX-06 is completed with explicit pass/fail evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-commands-dx/08-04-SUMMARY.md`
</output>
